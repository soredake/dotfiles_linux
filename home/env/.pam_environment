# https://github.com/ValveSoftware/steam-for-linux/issues/4161#issuecomment-220813769
#force_s3tc_enable=true

# life is strange is not starting when steam prefers host libraries
# https://wiki.gentoo.org/wiki/Steam/Games_troubleshooting#Steam_runtime
#STEAM_RUNTIME_PREFER_HOST_LIBRARIES=0

# https://wiki.archlinux.org/index.php/wine#Prevent_new_Wine_file_associations
WINEDLLOVERRIDES=winemenubuilder.exe=d

# gentoo is not setting this by default
# https://wiki.archlinux.org/index.php/XDG_Base_Directory_support
XDG_DATA_HOME			DEFAULT=@{HOME}/.local/share
XDG_CONFIG_HOME			DEFAULT=@{HOME}/.config
XDG_CACHE_HOME			DEFAULT=@{HOME}/.cache

# XDG's
DVDCSS_CACHE			DEFAULT=${XDG_CACHE_HOME}/dvdcss
TMUX_TMPDIR			DEFAULT=${XDG_RUNTIME_DIR}
#https://github.com/grawity/dotfiles/blob/master/.dotfiles.notes#L352
PUTTYDIR			DEFAULT=${XDG_CACHE_HOME}/putty
WGETRC				DEFAULT=${XDG_CONFIG_HOME}/wgetrc
XAUTHORITY			DEFAULT=${XDG_RUNTIME_DIR}/Xauthority

# Enable qt5ct for all qt programs
QT_QPA_PLATFORMTHEME=qt5ct

# Tor Browser settings
# Environment variable to disable the "TorButton" -> "Open Network Settings..."
# menu item.
TOR_NO_DISPLAY_NETWORK_SETTINGS=1
# Environment variable to disable the "TorButton" -> "Check for Updates..."
# menu item.
TOR_HIDE_UPDATE_CHECK_UI=1

# Steam is not quiting while you press quit, istead it just RE-LAUNCHES, so force the Steam window to close instead of minimize
# https://wiki.gentoo.org/wiki/Steam/Client_troubleshooting#Taskbar_button_persists_even_when_closed_or_minimzed
#STEAM_FRAME_FORCE_CLOSE=1

# not needed
LUTRIS_RUNTIME=0
