[Unit]
Description=Compton compositor (Nvidia driver)

[Service]
ExecStart=/usr/bin/compton --config %h/.config/compton.conf --backend xrender --unredir-if-possible
Restart=on-abort

[Install]
WantedBy=default.target
