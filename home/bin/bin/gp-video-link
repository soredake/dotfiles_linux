#!/usr/bin/env python3
# @TODO https://github.com/rg3/youtube-dl/issues/13889#issuecomment-340572485
import requests
import sys
link = sys.argv[1]
r = requests.get(link).text
for x in r.split('\n'):
    if 'video-downloads.googleusercontent.com' in x:
        v = x.split('"')[1]
        print(v)
